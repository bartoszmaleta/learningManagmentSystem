package com.company.models.users.notUsedModels.students;

import com.company.models.users.User;

public class Student extends User {

    public Student(int id, String username, String password, String name, String surname, String role) {
        super(id, username, password, name, surname, role);
    }
}
